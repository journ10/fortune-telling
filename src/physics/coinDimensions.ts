// Pure coin dimension constants shared by physics and geometry code.
// This module must stay free of three.js / DOM imports so headless
// physics code can use it in plain Node environments.

export const TABLETOP_COIN_RADIUS = 0.5;
export const TABLETOP_COIN_THICKNESS = 0.036;
